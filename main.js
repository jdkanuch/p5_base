// Author   : Josh Kanuch
// Date     : 2019-01-09
// P5.js template file

// Use to load data
function preload(){
    // most awesome way to load json.
    //data = loadJSON('data/data.json');
}

// Runs once
function setup(){
    var canvas = createCanvas(1200, 800);
    canvas.parent('content-div');

}

// Runs at frameRate
function draw() {
    background(51);
function draw() {
    background(51);
    //graph.simulate();
    graph.show();

}

}